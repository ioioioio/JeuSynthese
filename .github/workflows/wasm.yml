name: Wasm

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - uses: actions/checkout@v3
    - uses: mymindstorm/setup-emsdk@v14
    - name: Build
      run: |
        mkdir build
        cd build
        emcmake cmake ..
        emmake make